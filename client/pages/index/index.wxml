<!--client/pages/index/index.wxml-->
 <view class='page_row'>  
  <view class="search">  
    <view class="df search_arr">  
      <icon class="searchcion" size='20' type='search'></icon>  
      <input class="" placeholder="请输入关键字" bindinput="searchInput"/>  
    </view>  
  </view>  
  <view class='sousuo' bindtap="search">搜索</view>  
</view>  
<view class="" wx:for="{{movieData}}" wx:key="{{index}}">{{item.name}}</view>
